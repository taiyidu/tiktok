<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.taiyidu.taiyidu.gongsheng.mapper.LogDownloadMapper">
    <insert id="insert">
        insert into log_download(aweme_id,
                                 title,
                                 media_type,
                                 origin_url,
                                 download_urls,
                                 parse_status,
                                 error_msg,
                                 create_time,
                                 user_id)
                                 values(#{awemeId}, #{title}, #{mediaType}, #{originUrl}, #{downloadUrls},
                                 #{parseStatus}, #{errorMsg}, #{createTime},#{userId})
    </insert>
    <select id="showHistory" resultType="com.taiyidu.taiyidu.gongsheng.pojo.vo.HistoryRecordVo">
        select id, aweme_id, title, media_type, origin_url, create_time , download_urls from log_download
        where user_id = #{userId}
        order by id
    </select>
</mapper>
